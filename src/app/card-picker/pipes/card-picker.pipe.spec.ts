import { CardPickerPipe } from './card-picker.pipe';

describe('CardPickerPipe', () => {
  it('create an instance', () => {
    const pipe = new CardPickerPipe();
    expect(pipe).toBeTruthy();
  });
});
